{{-- <section id="achievement" class="py-32 px-4 md:px-8 bg-gradient-to-br from-gray-50 via-white to-gray-50">
    <div class="max-w-7xl mx-auto">
        <div class="text-center mb-16">
            <h2 class="animate-on-scroll text-5xl md:text-6xl font-black text-primary mb-6 opacity-0 transition-all duration-1000" style="transform: translateY(40px);">
                Pencapaian Kami
            </h2>
            <p class="animate-on-scroll text-xl text-gray-600 max-w-2xl mx-auto font-light opacity-0 transition-all duration-1000" style="transform: translateY(40px); transition-delay: 200ms;">
                Komitmen nyata dalam memberdayakan generasi pemimpin masa depan
            </p>
        </div>

        {{-- Stats Grid --}}
        {{-- <div class="grid md:grid-cols-4 gap-8">
            @php
                $achievements = [
                    ['stat' => '15+', 'label' => 'Penghargaan Nasional', 'icon' => '🏆'],
                    ['stat' => '50K+', 'label' => 'Mahasiswa Terdampak', 'icon' => '👥'],
                    ['stat' => '12', 'label' => 'Tahun Berkontribusi', 'icon' => '📅'],
                    ['stat' => '200+', 'label' => 'Event Per Tahun', 'icon' => '🎯'],
                ];
            @endphp
            @foreach($achievements as $index => $item)
                <div class="animate-on-scroll group p-8 rounded-2xl bg-white border border-gray-200 hover-lift text-center opacity-0 transition-all duration-700" style="transform: scale(0.95); transition-delay: {{ $index * 150 }}ms;">
                    <div class="text-5xl mb-4 transform group-hover:scale-125 transition-transform">{{ $item['icon'] }}</div>
                    <div class="text-4xl font-black text-accent mb-3">{{ $item['stat'] }}</div>
                    <p class="text-gray-600 font-semibold">{{ $item['label'] }}</p>
                </div>
            @endforeach
        </div>
    </div>
</section> --}} 
